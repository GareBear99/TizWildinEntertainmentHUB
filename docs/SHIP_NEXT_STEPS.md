# Ship Next Steps

1. Enable GitHub Pages from `/docs`.
2. Add the recommended repo description, website, and topics from `docs/SEO_PROMOTION.md`.
3. Tag a cleanup release after verifying the site and tests.
4. Add direct release links as product repos publish binaries.
5. Connect FreeEQ8 demo tracks and Release Vault proof rows.
6. Add signed builds only when platform signing is ready.
